import 'package:flutter/material.dart';

class VictoryColors {
  // Backgrounds
  static const Color background = Color(0xFF0D1117);
  static const Color backgroundCard = Color(0xFF1A2330);
  
  // Accents
  static const Color accent = Color(0xFFD4875A); // Bronze/Gold accent
  static const Color success = Color(0xFF4ADE80);
  static const Color gold = Color(0xFFFFD700);
  
  // Text
  static const Color textPrimary = Colors.white;
  static const Color textSecondary = Color(0xFF90989F);
  static const Color textMuted = Color(0xFF6878A0);
  
  // Borders & Dividers
  static const Color border = Color(0xFF2A3444);
  
  // Rank Colors
  static const Color rankS = Color(0xFFFFD700);
  static const Color rankA = Color(0xFFD4875A);
  static const Color rankB = Color(0xFF6878A0);

  // Animation Timings
  static const Duration phaseTransition = Duration(milliseconds: 600);
  static const Duration staggerDelay = Duration(milliseconds: 250);
  static const Duration countingDuration = Duration(seconds: 1);
}
